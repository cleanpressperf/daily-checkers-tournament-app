'use client'

import Link from 'next/link'
import { useEffect, useState } from 'react'
import { deductBalance, getBalance } from '@/lib/wallet'
export default function WithdrawPage(){const [balance,setBalance]=useState(0);const [amount,setAmount]=useState('');const [message,setMessage]=useState('');useEffect(()=>{const sync=()=>setBalance(getBalance());sync();window.addEventListener('wallet-change',sync);return()=>window.removeEventListener('wallet-change',sync)},[]);function request(){const value=Number(amount);if(!Number.isInteger(value)||value<=0||value>balance){setMessage('Enter a valid whole amount within your balance.');return}if(!deductBalance(value)){setMessage('Unable to reserve that amount.');return}setBalance(getBalance());setAmount('');setMessage(`Withdrawal pending. ${value.toLocaleString()} coins are reserved for 24 hours.`)}return <main className="min-h-screen bg-[#080808] px-5 py-8 text-white"><div className="mx-auto max-w-lg"><Link href="/arena" className="text-sm text-white/50">Back to Arena</Link><h1 className="mt-10 text-4xl font-black">Withdraw coins</h1><p className="mt-3 text-white/50">Requests remain pending for 24 hours while they are reviewed.</p><div className="mt-6 rounded-2xl border border-[#d6ff38]/30 bg-[#d6ff38]/10 p-6"><p className="text-xs font-bold uppercase tracking-widest text-[#d6ff38]">Available balance</p><p className="mt-2 text-4xl font-black text-[#d6ff38]">N{balance.toLocaleString()}</p></div><label className="mt-8 block text-sm font-bold" htmlFor="withdraw-amount">Amount</label><input id="withdraw-amount" inputMode="numeric" value={amount} onChange={e=>{setAmount(e.target.value);setMessage('')}} className="mt-2 w-full rounded-xl border border-white/15 bg-[#111] px-4 py-3 text-white outline-none focus:border-[#d6ff38]" placeholder="Enter amount"/><button onClick={request} className="mt-4 w-full rounded-xl bg-[#d6ff38] px-5 py-3 font-black text-black">REQUEST WITHDRAWAL</button>{message&&<p role="status" className="mt-4 text-center text-sm text-white/70">{message}</p>}</div></main>}
